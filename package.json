{
  "name": "minetrigger",
  "version": "1.0.0",
  "description": "Minesweeper 2.0",
  "scripts": {
    "build": "tsc",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@capacitor/cli": "^5.6.1"
  },
  "dependencies": {
    "@capacitor/core": "^5.6.1"
  }
}
