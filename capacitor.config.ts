import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.minetrigger',
  appName: 'Minetrigger',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
